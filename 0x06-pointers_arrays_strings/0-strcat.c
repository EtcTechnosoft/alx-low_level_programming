#include "makit.h"

/**
 * *_strcat - Concatenates two pointers
 *
 *@src : Has the string to be concatenated
 @dest: The pointer to hold the concatenation
 */
char *_strcat(char *dest, char *src)
{
	int a;
	int b;

	for (a = 0, dest[a] a != 0, a++)
	{
	}
	for (b = 0, src[b] b != 0, b++)
	{
		dest[a] = src[b];
	}
	return (dest);
}
