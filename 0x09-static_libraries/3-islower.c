#include "main.h"
/**
* _islower - check for lowercase alphabets
* @c: The character for the case
*
* Description: Checks for lowercase  character
*
* Return:1(success) or 0(otherwise)
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
